import { assert, expect } from "chai";
import { describe } from "mocha";

describe('check if returns true', () => {
    it('checking first number', () => {
        assert.strictEqual(true, true, 'these booleans are strictly equal');
    });
});